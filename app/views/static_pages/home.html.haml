- if logged_in?
  .row
    %aside.col-md-4
      %section.user_info
        = render 'shared/user_info'
      %section.microposts_form
        = render 'shared/micropost_form'
- else
  = render 'static_pages/welcome_page'
